<Window x:Class="MyProject.Grades"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyProject"
        mc:Ignorable="d"
        Title="טבלת ציונים" Height="450" Width="450">
    <Grid>
        <DataGrid Height="350" Width="200" Margin="0,50,0,0" Name="GradesDisplay"  AutoGenerateColumns="False" Background="Honeydew">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Grade}" Header="ציון" Width="120"/>
                <DataGridTextColumn Binding="{Binding Grrde_Id}" Header="מספר מבחן" Width="80"/>
            </DataGrid.Columns>
            </DataGrid>
    </Grid>
</Window>
